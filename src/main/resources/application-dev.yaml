spring:
  datasource:
    url: jdbc:sqlite:./data/saldo-dev.db
    driver-class-name: org.sqlite.JDBC
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
  flyway:
    locations: classpath:db/migration/sqlite
  graphql:
    graphiql:
      enabled: true